const input = document.querySelector('.input')

function button() {
    if (input.displayContent === 'none') {
        button.style.display = 'none'
        
    }else{
        button.style.visibility = 'block'
    }
    
}